---
layout: layout.njk
---
{%- for post in collections.recent -%}
  <article>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <div class='post-meta'>
      {% timeTagForDate post.date %}
      {% tagList post.data.tags | except("blog") %}
    </div>
    {{ post.templateContent | safe }}
  </article>
{%- endfor -%}
